import { PlacaAutoMercosulPipe } from './placa-auto-mercosul.pipe';

describe('PlacaAutoMercosulPipe', () => {
  it('create an instance', () => {
    const pipe = new PlacaAutoMercosulPipe();
    expect(pipe).toBeTruthy();
  });
});
