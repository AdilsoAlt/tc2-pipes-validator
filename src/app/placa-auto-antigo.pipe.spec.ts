import { PlacaAutoAntigoPipe } from './placa-auto-antigo.pipe';

describe('PlacaAutoAntigoPipe', () => {
  it('create an instance', () => {
    const pipe = new PlacaAutoAntigoPipe();
    expect(pipe).toBeTruthy();
  });
});
